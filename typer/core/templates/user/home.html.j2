<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Typer - start</title>
</head>
<body>
<h1>Witaj, {{ username }}</h1>
<hr>
<h2>Twoje portfele:</h2>
<ul>
    {% for wallet in wallets %}
    <li><a href="/typer/wallet/{{ wallet.id }}">{{  wallet.name  }}</a></li>
    {% endfor %}
</ul>
<hr>
<h2>Dostępne wydarzenia:</h2>
    {% for event in events %}
    <li><a href="/typer/event/{{ event.id }}">{{ event.name }}</a></li>
    {% endfor %}
</body>
</html>